

void setup() {
  size(1200, 600);
}

void draw() {
  //draw background bottom layer
  background(0);
  //call ship.draw() on each ship in the set
  //call drawMoveArc on current selected ship if hasMoved==0
  //call projectile.draw() on the set of projectiles
  // call effects.draw() on the set of effects 
}
